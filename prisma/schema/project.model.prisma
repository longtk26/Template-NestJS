model Project {
    id             String   @id @default(uuid())
    name           String
    description    String
    availableSlots Int
    createdAt      DateTime @default(now())
    updatedAt      DateTime @default(now()) @updatedAt

    @@map("projects")
}
